# Web Clipper Environment Configuration
# This file contains sensitive configuration values
# Keep this file secure (mode 640, owned by root:web-clipper)

# Server Configuration
PORT=3000
SERVER_BASE_URL=http://localhost:3000

# OAuth Configuration
# Supported providers: keycloak, google
OAUTH_PROVIDER=keycloak
OAUTH_CLIENT_ID=your-client-id
OAUTH_CLIENT_SECRET=your-client-secret
OAUTH_REDIRECT_URL=http://localhost:3000/auth/callback

# Keycloak Configuration (if using keycloak provider)
KEYCLOAK_REALM=web-clipper
KEYCLOAK_BASE_URL=https://auth.example.com

# JWT Secret (REQUIRED - generate with: openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_GENERATE_A_SECURE_SECRET

# Database path (default: /var/lib/web-clipper/data/clipper.sqlite3)
# DATABASE_URL=sqlite3:///var/lib/web-clipper/data/clipper.sqlite3
