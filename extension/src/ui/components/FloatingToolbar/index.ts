/**
 * Floating Toolbar Component
 *
 * A floating toolbar that positions itself relative to an anchor element
 * with automatic viewport-aware positioning and keyboard navigation.
 */

export {
  FloatingToolbar,
  createFloatingToolbar,
  type FloatingToolbarProps,
  type ToolbarAction,
  type ToolbarSeparator,
  type ToolbarItem,
  type ToolbarActionEvent,
} from './FloatingToolbar.js';

export { default as styles } from './FloatingToolbar.css';
